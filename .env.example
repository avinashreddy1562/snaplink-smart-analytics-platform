# Django / backend (Phase 1 will load these values.)
DJANGO_SECRET_KEY=replace-with-a-long-local-development-secret
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
POSTGRES_DB=snaplink
POSTGRES_USER=snaplink
POSTGRES_PASSWORD=change-me-before-use
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Browser-facing services
VITE_API_BASE_URL=http://localhost:8000/api
WORKER_BACKEND_URL=http://localhost:8000
